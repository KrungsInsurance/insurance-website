# Project instructions

Read `Rule.md` before working in this project. For UI tasks, also read the relevant sections of `DESIGN.md` and the installed `baseline-ui` skill. Apply the installed `ponytail` skill to coding tasks. User instructions take precedence over these project references.

Implementation specification: `plan.md`. Read sections 0–4 once, then the active phase and its linked contracts/tests. Track actual progress in section 12. Sync flow or phase changes to the Miro plan linked in section 16.
